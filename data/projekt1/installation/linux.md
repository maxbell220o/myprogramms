# Installation (Linux)

1. Python installieren:

sudo apt install python3 python3-pip

2. Repo klonen:

git clone <dein-link>

3. Installieren:

pip3 install -r requirements.txt

4. Starten:

python3 main.py
